# ️ Whale Ecosystem Audit Dossier (Q4 2026 Trail of Bits Prep)

This document maps the exact threat surface and invariant requirements for the formal auditing of the Whale Ecosystem.

## 1. System Overview
**Project:** Whale System Analytics Network
**Target Network:** Base (L2) / Polygon (L2) / Eigenlayer (L1)
**Scope:** 
- `WhaleDeadmanSwitch.sol`
- `HumanTimeLock.sol`
- `WhaleAVS.sol`
- `WhaleZKVerifier.sol`

## 2. Roles and Permissions
1. **Admin / Architect (Multisig):** Can pause AVS operations, adjust time-lock durations, and slash Byzantine AVS nodes.
2. **AVS Node (System Array):** Mocks ZK-SNARK verifiers and signs off on mathematical `G(t)/E(t)` thermodynamic metrics.
3. **Backup Heir:** Has 0 permissions until the Deadman Switch timer hits zero, at which point they can claim control.

## 3. Formal Invariants & Slither Configuration

### Deadman Switch Invariants
1. **Inv 1:** `claimOwnership` MUST revert if `block.timestamp < lastPing + gracePeriod`.
2. **Inv 2:** The `gracePeriod` MUST NEVER be set below `1 days` to prevent immediate hostile takeover.

### AVS Invariants
1. **Inv 3:** `attestSignal` MUST revert if the node's `stakedAmount` is less than `MIN_STAKE` (10,000 tokens).
2. **Inv 4:** Double-voting by an `msg.sender` on a single `txHash` MUST be impossible.

### Slither Prep Command
```bash
slither . --print human-summary --exclude-dependencies --solc-remaps "@openzeppelin=node_modules/@openzeppelin"
```

## 4. Known Centralization Vectors
- **Acknowledge:** The `slashOperator` function in `WhaleAVS.sol` is currently `onlyOwner`. In Phase 8, this will transition to an automated programmatic slashing condition directly tied to the ZK Verifier output without admin intervention.

---
*Generated by Whale Network Core 3.0.0*
