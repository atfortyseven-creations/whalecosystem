[providers]
node = true

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx prisma db push --accept-data-loss",
  "npx next build"
]

[start]
cmd = "sh ./start.sh"
